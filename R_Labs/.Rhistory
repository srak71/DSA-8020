xg <- seq(min(teengamb$income), max(teengamb$income), length.out = 200)
# 95% CI
GAM_Pred <- predict(gam_fit, newdata = data.frame(income = xg), se.fit = TRUE)
data_plt <- data.frame(income = xg, fit = GAM_Pred$fit, lower = GAM_Pred$fit - 2 * GAM_Pred$se.fit,
upper = GAM_Pred$fit + 2 * GAM_Pred$se.fit)
# Plot with 95% CI
ggplot() +
geom_point(data = teengamb, aes(x = income, y = gamble), color = "blue") +
geom_line(data = data_plt, aes(x = income, y = fit), color = "red", size = 1) +
geom_ribbon(data = data_plt, aes(x = income, ymin = lower, ymax = upper), alpha = 0.5) +
labs(title = "GAM Spline (95% CI)", x = "income", y = "$ gambled") +
theme_minimal()
data(meatspec, package = "faraway")
train <- 1:150; test <- 151:215
trainmeat <- meatspec[train,]
testmeat <- meatspec[test,]
lmFit <- lm(fat ~ ., data = trainmeat)
# Define a function to calculate RMSE
rmse <- function(pred, obs) sqrt(mean((pred - obs)^2))
# Computing RMSE for the training set
rmse(fitted(lmFit), trainmeat$fat)
gam_fit <- gam(gamble ~ s(income), data = teengamb)
# all.knot to remove duplicates
smooth_spline_fit <- smooth.spline(teengamb$income, teengamb$gamble, cv = TRUE, all.knots = TRUE)
# sequence income
xg <- seq(min(teengamb$income), max(teengamb$income), length.out = 200)
# 95% CI
GAM_Pred <- predict(gam_fit, newdata = data.frame(income = xg), se.fit = TRUE)
data_plt <- data.frame(income = xg, fit = GAM_Pred$fit, lower = GAM_Pred$fit - 2 * GAM_Pred$se.fit,
upper = GAM_Pred$fit + 2 * GAM_Pred$se.fit)
# Plot with 95% CI
ggplot() +
geom_point(data = teengamb, aes(x = income, y = gamble), color = "blue") +
geom_line(data = data_plt, aes(x = income, y = fit), color = "red", linewidth = 1) +
geom_ribbon(data = data_plt, aes(x = income, ymin = lower, ymax = upper), alpha = 0.5) +
labs(title = "GAM Spline (95% CI)", x = "income", y = "$ gambled") +
theme_minimal()
gam_fit <- gam(gamble ~ s(income), data = teengamb)
# all.knot to remove duplicates
smooth_spline_fit <- smooth.spline(teengamb$income, teengamb$gamble, all.knots = TRUE)
# sequence income
xg <- seq(min(teengamb$income), max(teengamb$income), length.out = 200)
# 95% CI
GAM_Pred <- predict(gam_fit, newdata = data.frame(income = xg), se.fit = TRUE)
data_plt <- data.frame(income = xg, fit = GAM_Pred$fit, lower = GAM_Pred$fit - 2 * GAM_Pred$se.fit,
upper = GAM_Pred$fit + 2 * GAM_Pred$se.fit)
# Plot with 95% CI
ggplot() +
geom_point(data = teengamb, aes(x = income, y = gamble), color = "blue") +
geom_line(data = data_plt, aes(x = income, y = fit), color = "red", linewidth = 1) +
geom_ribbon(data = data_plt, aes(x = income, ymin = lower, ymax = upper), alpha = 0.5) +
labs(title = "GAM Spline (95% CI)", x = "income", y = "$ gambled") +
theme_minimal()
ggplot(teengamb, aes(x = income, y = gamble)) +
geom_point(color = "blue", size = 3) +
labs(title = "Scatterplot (amt gambled by income)", x = "income", y = "$ gambled") +
theme_minimal()
ggplot(teengamb, aes(x = income, y = gamble)) +
geom_point(color = "blue", size = 3) +
labs(title = "Scatterplot ($ gambled by income)", x = "income", y = "$ gambled") +
theme_minimal()
gam_fit <- gam(gamble ~ s(income), data = teengamb)
# all.knot to remove dupes
smooth_spline_fit <- smooth.spline(teengamb$income, teengamb$gamble, all.knots = TRUE)
# sequence income
xg <- seq(min(teengamb$income), max(teengamb$income), length.out = 200)
# 95% CI
GAM_Pred <- predict(gam_fit, newdata = data.frame(income = xg), se.fit = TRUE)
data_plt <- data.frame(income = xg, fit = GAM_Pred$fit, lower = GAM_Pred$fit - 2 * GAM_Pred$se.fit,
upper = GAM_Pred$fit + 2 * GAM_Pred$se.fit)
# Plot with 95% CI
ggplot() +
geom_point(data = teengamb, aes(x = income, y = gamble), color = "blue") +
geom_line(data = data_plt, aes(x = income, y = fit), color = "red", linewidth = 1) +
geom_ribbon(data = data_plt, aes(x = income, ymin = lower, ymax = upper)) +
labs(title = "GAM Spline (95% CI)", x = "income", y = "$ gambled") +
theme_minimal()
gam_fit <- gam(gamble ~ s(income), data = teengamb)
# all.knot to remove dupes
smooth_spline_fit <- smooth.spline(teengamb$income, teengamb$gamble, all.knots = TRUE)
# sequence income
xg <- seq(min(teengamb$income), max(teengamb$income), length.out = 200)
# 95% CI
GAM_Pred <- predict(gam_fit, newdata = data.frame(income = xg), se.fit = TRUE)
data_plt <- data.frame(income = xg, fit = GAM_Pred$fit, lower = GAM_Pred$fit - 2 * GAM_Pred$se.fit,
upper = GAM_Pred$fit + 2 * GAM_Pred$se.fit)
# Plot with 95% CI
ggplot() +
geom_point(data = teengamb, aes(x = income, y = gamble), color = "blue") +
geom_line(data = data_plt, aes(x = income, y = fit), color = "red", linewidth = 1) +
geom_ribbon(data = data_plt, aes(x = income, ymin = lower, ymax = upper), alpha = 0.5) +
labs(title = "GAM Spline (95% CI)", x = "income", y = "$ gambled") +
theme_minimal()
gam_fit <- gam(gamble ~ s(income), data = teengamb)
# all.knot to remove dupes
smooth_spline_fit <- smooth.spline(teengamb$income, teengamb$gamble, all.knots = TRUE)
# sequence income
xg <- seq(min(teengamb$income), max(teengamb$income), length.out = 200)
# 95% CI
GAM_Pred <- predict(gam_fit, newdata = data.frame(income = xg), se.fit = TRUE)
data_plt <- data.frame(income = xg, fit = GAM_Pred$fit, lower = GAM_Pred$fit - 2 * GAM_Pred$se.fit,
upper = GAM_Pred$fit + 2 * GAM_Pred$se.fit)
# Plot with 95% CI
ggplot() +
geom_point(data = teengamb, aes(x = income, y = gamble), color = "blue") +
geom_line(data = data_plt, aes(x = income, y = fit), color = "red", linewidth = 1) +
geom_ribbon(data = data_plt, aes(x = income, ymin = lower, ymax = upper), alpha = 0.1) +
labs(title = "GAM Spline (95% CI)", x = "income", y = "$ gambled") +
theme_minimal()
gam_fit <- gam(gamble ~ s(income), data = teengamb)
# all.knot to remove dupes
smooth_spline_fit <- smooth.spline(teengamb$income, teengamb$gamble, all.knots = TRUE)
# sequence income
xg <- seq(min(teengamb$income), max(teengamb$income), length.out = 200)
# 95% CI
GAM_Pred <- predict(gam_fit, newdata = data.frame(income = xg), se.fit = TRUE)
data_plt <- data.frame(income = xg, fit = GAM_Pred$fit, lower = GAM_Pred$fit - 2 * GAM_Pred$se.fit,
upper = GAM_Pred$fit + 2 * GAM_Pred$se.fit)
# Plot with 95% CI
ggplot() +
geom_point(data = teengamb, aes(x = income, y = gamble), color = "blue") +
geom_line(data = data_plt, aes(x = income, y = fit), color = "red", linewidth = 1) +
geom_ribbon(data = data_plt, aes(x = income, ymin = lower, ymax = upper), alpha = 0.5) +
labs(title = "GAM Spline (95% CI)", x = "income", y = "$ gambled") +
theme_minimal()
data_plt <- data.frame(income = xg, fit = GAM_Pred$fit, lower = GAM_Pred$fit - 2 * GAM_Pred$se.fit,
upper = GAM_Pred$fit + 2 * GAM_Pred$se.fit)
# Plot with 95% CI
ggplot() +
geom_point(data = teengamb, aes(x = income, y = gamble), color = "blue") +
geom_line(data = data_plt, aes(x = income, y = fit), color = "red", linewidth = 1) +
geom_ribbon(data = data_plt, aes(x = income, ymin = lower, ymax = upper), alpha = 0.5) +
labs(title = "GAM Spline (95% CI)", x = "income", y = "$ gambled") +
theme_minimal()
knitr::opts_chunk$set(echo = TRUE)
library(dplyr)
library(ggplot2)
library(faraway)
library(splines)
library(mgcv)
data(teengamb)
head(teengamb)
ggplot(teengamb, aes(x = income, y = gamble)) +
geom_point(color = "blue", size = 3) +
labs(title = "Scatterplot ($ gambled by income)", x = "income", y = "$ gambled") +
theme_minimal()
# regression spline w/ df = 8
RegSplineFit <- lm(gamble ~ bs(income, df = 8), data = teengamb)
# sequence income
xg <- seq(min(teengamb$income), max(teengamb$income))
# 95% CI
RegSplinePred <- predict(RegSplineFit, newdata = data.frame(income = xg), interval = "confidence")
data_plt <- data.frame(income = xg, fit = RegSplinePred[, "fit"], lower = RegSplinePred[, "lwr"], upper = RegSplinePred[, "upr"])
ggplot() +
geom_point(data = teengamb, aes(x = income, y = gamble), color = "blue") +
geom_line(data = data_plt, aes(x = income, y = fit), color = "red") +
geom_ribbon(data = data_plt, aes(x = income, ymin = lower, ymax = upper), alpha = 0.5) +
labs(title = "Regression Spline (95% CI)", x = "income", y = "$ gambled")
gam_fit <- gam(gamble ~ s(income), data = teengamb)
# all.knot to remove dupes
smooth_spline_fit <- smooth.spline(teengamb$income, teengamb$gamble, all.knots = TRUE)
# sequence income
xg <- seq(min(teengamb$income), max(teengamb$income), length.out = 200)
# 95% CI
GAM_Pred <- predict(gam_fit, newdata = data.frame(income = xg), se.fit = TRUE)
data_plt <- data.frame(income = xg, fit = GAM_Pred$fit, lower = GAM_Pred$fit - 2 * GAM_Pred$se.fit,
upper = GAM_Pred$fit + 2 * GAM_Pred$se.fit)
# Plot with 95% CI
ggplot() +
geom_point(data = teengamb, aes(x = income, y = gamble), color = "blue") +
geom_line(data = data_plt, aes(x = income, y = fit), color = "red", linewidth = 1) +
geom_ribbon(data = data_plt, aes(x = income, ymin = lower, ymax = upper), alpha = 0.5) +
labs(title = "GAM Spline (95% CI)", x = "income", y = "$ gambled") +
theme_minimal()
data(meatspec, package = "faraway")
train <- 1:150; test <- 151:215
trainmeat <- meatspec[train,]
testmeat <- meatspec[test,]
lmFit <- lm(fat ~ ., data = trainmeat)
# Define a function to calculate RMSE
rmse <- function(pred, obs) sqrt(mean((pred - obs)^2))
# Computing RMSE for the training set
rmse(fitted(lmFit), trainmeat$fat)
lmFit <- lm(fat ~ ., data = trainmeat)
# Define a function to calculate RMSE
rmse <- function(pred, obs) sqrt(mean((pred - obs)^2))
# Computing RMSE for the training set
rmse(fitted(lmFit), trainmeat$fat)
# RMSE for test set
rmse(predict(lmFit, newdata = testmeat), testmeat$fat)
data(meatspec, package = "faraway")
train <- 1:150
test <- 151:215
trainmeat <- meatspec[train,]
testmeat <- meatspec[test,]
lmFit <- lm(fat ~ ., data = trainmeat)
# Define a function to calculate RMSE
rmse <- function(pred, obs) sqrt(mean((pred - obs)^2))
# Computing RMSE for the training set
rmse(fitted(lmFit), trainmeat$fat)
# RMSE for test set
rmse(predict(lmFit, newdata = testmeat), testmeat$fat)
# fat is reponse var, only include in y
x_train <- as.matrix(trainmeat[, -1])
y_train <- trainmeat$fat
x_test <- as.matrix(testmeat[, -1])
y_test <- testmeat$fat
# cross validation with alpha = 0 for ridge regression
cv_ridge <- cv.glmnet(x_train, y_train, alpha = 0, standardize = TRUE)
knitr::opts_chunk$set(echo = TRUE)
library(dplyr)
library(ggplot2)
library(faraway)
library(splines)
library(mgcv)
library(glmnet)
data(teengamb)
head(teengamb)
ggplot(teengamb, aes(x = income, y = gamble)) +
geom_point(color = "blue", size = 3) +
labs(title = "Scatterplot ($ gambled by income)", x = "income", y = "$ gambled") +
theme_minimal()
# regression spline w/ df = 8
RegSplineFit <- lm(gamble ~ bs(income, df = 8), data = teengamb)
# sequence income
xg <- seq(min(teengamb$income), max(teengamb$income))
# 95% CI
RegSplinePred <- predict(RegSplineFit, newdata = data.frame(income = xg), interval = "confidence")
data_plt <- data.frame(income = xg, fit = RegSplinePred[, "fit"], lower = RegSplinePred[, "lwr"], upper = RegSplinePred[, "upr"])
ggplot() +
geom_point(data = teengamb, aes(x = income, y = gamble), color = "blue") +
geom_line(data = data_plt, aes(x = income, y = fit), color = "red") +
geom_ribbon(data = data_plt, aes(x = income, ymin = lower, ymax = upper), alpha = 0.5) +
labs(title = "Regression Spline (95% CI)", x = "income", y = "$ gambled")
gam_fit <- gam(gamble ~ s(income), data = teengamb)
# all.knot to remove dupes
smooth_spline_fit <- smooth.spline(teengamb$income, teengamb$gamble, all.knots = TRUE)
# sequence income
xg <- seq(min(teengamb$income), max(teengamb$income), length.out = 200)
# 95% CI
GAM_Pred <- predict(gam_fit, newdata = data.frame(income = xg), se.fit = TRUE)
data_plt <- data.frame(income = xg, fit = GAM_Pred$fit, lower = GAM_Pred$fit - 2 * GAM_Pred$se.fit,
upper = GAM_Pred$fit + 2 * GAM_Pred$se.fit)
# Plot with 95% CI
ggplot() +
geom_point(data = teengamb, aes(x = income, y = gamble), color = "blue") +
geom_line(data = data_plt, aes(x = income, y = fit), color = "red", linewidth = 1) +
geom_ribbon(data = data_plt, aes(x = income, ymin = lower, ymax = upper), alpha = 0.5) +
labs(title = "GAM Spline (95% CI)", x = "income", y = "$ gambled") +
theme_minimal()
data(meatspec, package = "faraway")
train <- 1:150
test <- 151:215
trainmeat <- meatspec[train,]
testmeat <- meatspec[test,]
lmFit <- lm(fat ~ ., data = trainmeat)
# Define a function to calculate RMSE
rmse <- function(pred, obs) sqrt(mean((pred - obs)^2))
# Computing RMSE for the training set
rmse(fitted(lmFit), trainmeat$fat)
# RMSE for test set
rmse(predict(lmFit, newdata = testmeat), testmeat$fat)
# fat is reponse var, only include in y
x_train <- as.matrix(trainmeat[, -1])
y_train <- trainmeat$fat
x_test <- as.matrix(testmeat[, -1])
y_test <- testmeat$fat
# cross validation with alpha = 0 for ridge regression
cv_ridge <- cv.glmnet(x_train, y_train, alpha = 0, standardize = TRUE)
# getting best lambda
best_lambda <- cv_ridge$lambda.min
print(c("Best Lambda: ", best_lambda))
# predict on training
train_pred <- predict(cv_ridge, s = best_lambda, newx = x_train)
train_rmse <- rmse(train_pred, y_train)
print(c("Training RMSE: ", train_rmse))
# pred on test set
test_pred <- predict(cv_ridge, s = best_lambda, newx = x_test)
test_rmse <- rmse(test_pred, y_test)
print(c("Test RMSE: ", test_rmse))
# now using alpha = 1 for lasso regression
cv_lasso <- cv.glmnet(x_train, y_train, alpha = 1, standardize = TRUE)
# getting new best lambda
best_lambda <- cv_lasso$lambda.min
print(c("Best Lambda: ", best_lambda))
# predict on training
train_pred <- predict(cv_lasso, s = best_lambda, newx = x_train)
train_rmse <- rmse(train_pred, y_train)
print(c("Lasso Regression Training RMSE: ", train_rmse))
# pred on test set
test_pred <- predict(cv_lasso, s = best_lambda, newx = x_test)
test_rmse <- rmse(test_pred, y_test)
print(c("Test RMSE: ", test_rmse))
# now using alpha = 1 for lasso regression
cv_lasso <- cv.glmnet(x_train, y_train, alpha = 1, standardize = TRUE)
# getting new best lambda
best_lambda <- cv_lasso$lambda.min
print(c("Best Lambda: ", best_lambda))
# predict on training
train_pred <- predict(cv_lasso, s = best_lambda, newx = x_train)
train_rmse <- rmse(train_pred, y_train)
print(c("Lasso Training RMSE: ", train_rmse))
# pred on test set
test_pred <- predict(cv_lasso, s = best_lambda, newx = x_test)
test_rmse <- rmse(test_pred, y_test)
print(c("Lasso Test RMSE: ", test_rmse))
x_full <- as.matrix(meatspec[, -1])
y_full <- meatspec$fat
lasso_full <- glmnet(x_full, y_full, alpha = 1, lambda = best_lambda, standardize = TRUE)
# get coeff
lasso_coefs <- coef(lasso_full)
non_zero_count <- sum(lasso_coefs[-1, ] != 0) # -1 to remove predictor
print(c("# of non-zero coefficients: ", lasso_coefs))
x_full <- as.matrix(meatspec[, -1])
y_full <- meatspec$fat
lasso_full <- glmnet(x_full, y_full, alpha = 1, lambda = best_lambda, standardize = TRUE)
# get coeff
lasso_coefs <- coef(lasso_full)
non_zero_count <- sum(lasso_coefs[-1, ] != 0) # -1 to remove predictor
print(c("# of non-zero coefficients: ", non_zero_count))
x_full <- as.matrix(meatspec[, -1])
y_full <- meatspec$fat
lasso_full <- glmnet(x_full, y_full, alpha = 1, lambda = best_lambda, standardize = TRUE)
# get coeff
lasso_coefs <- coef(lasso_full)
non_zero_coeff <- sum(lasso_coefs[-1, ] != 0) # -1 to remove predictor
print(c("# of non-zero coefficients: ", non_zero_coeff))
x_full <- as.matrix(meatspec[, -1])
y_full <- meatspec$fat
lasso_full <- glmnet(x_full, y_full, alpha = 1, lambda = best_lambda, standardize = TRUE)
# get coeff
lasso_coefs <- coef(lasso_full)
non_zero_coeff <- sum(lasso_coefs[-1, ] != 0) # -1 to remove predictor
selected_preds <- rownames(lasso_coefs)[which(lasso_coefs[-1, ] != 0) + 1]
print(c("# of non-zero coefficients: ", non_zero_coeff))
print(c("non-zero coefficients: ", selected_preds))
x_full <- as.matrix(meatspec[, -1])
y_full <- meatspec$fat
lasso_full <- glmnet(x_full, y_full, alpha = 1, lambda = best_lambda, standardize = TRUE)
# get coeff
lasso_coefs <- coef(lasso_full)
non_zero_coeff <- sum(lasso_coefs[-1, , drop = FALSE] != 0)  # -1 to remove predictor
selected_preds <- rownames(lasso_coefs)[which(lasso_coefs[-1, , drop = FALSE] != 0) + 1]
print(c("# of non-zero coefficients: ", non_zero_coeff))
print(c("non-zero coefficients: ", selected_preds))
x_full <- as.matrix(meatspec[, -1])
y_full <- meatspec$fat
lasso_full <- glmnet(x_full, y_full, alpha = 1, lambda = best_lambda, standardize = TRUE)
# get coeff
lasso_coefs <- coef(lasso_full)
non_zero_coeff <- sum(lasso_coefs[-1, , drop = FALSE] != 0)  # -1 to remove predictor
selected_preds <- rownames(lasso_coefs)[which(lasso_coefs[-1, , drop = FALSE] != 0) + 1]
print(c("# of non-zero coefficients: ", non_zero_coeff))
print(c("non-zero coefficients: ", selected_preds))
knitr::opts_chunk$set(echo = TRUE)
library(dplyr)
library(ggplot2)
library(faraway)
library(splines)
library(mgcv)
library(glmnet)
data(teengamb)
head(teengamb)
ggplot(teengamb, aes(x = income, y = gamble)) +
geom_point(color = "blue", size = 3) +
labs(title = "Scatterplot ($ gambled by income)", x = "income", y = "$ gambled") +
theme_minimal()
# regression spline w/ df = 8
RegSplineFit <- lm(gamble ~ bs(income, df = 8), data = teengamb)
# sequence income
xg <- seq(min(teengamb$income), max(teengamb$income))
# 95% CI
RegSplinePred <- predict(RegSplineFit, newdata = data.frame(income = xg), interval = "confidence")
data_plt <- data.frame(income = xg, fit = RegSplinePred[, "fit"], lower = RegSplinePred[, "lwr"], upper = RegSplinePred[, "upr"])
ggplot() +
geom_point(data = teengamb, aes(x = income, y = gamble), color = "blue") +
geom_line(data = data_plt, aes(x = income, y = fit), color = "red") +
geom_ribbon(data = data_plt, aes(x = income, ymin = lower, ymax = upper), alpha = 0.5) +
labs(title = "Regression Spline (95% CI)", x = "income", y = "$ gambled")
gam_fit <- gam(gamble ~ s(income), data = teengamb)
# all.knot to remove dupes
smooth_spline_fit <- smooth.spline(teengamb$income, teengamb$gamble, all.knots = TRUE)
# sequence income
xg <- seq(min(teengamb$income), max(teengamb$income), length.out = 200)
# 95% CI
GAM_Pred <- predict(gam_fit, newdata = data.frame(income = xg), se.fit = TRUE)
data_plt <- data.frame(income = xg, fit = GAM_Pred$fit, lower = GAM_Pred$fit - 2 * GAM_Pred$se.fit,
upper = GAM_Pred$fit + 2 * GAM_Pred$se.fit)
# Plot with 95% CI
ggplot() +
geom_point(data = teengamb, aes(x = income, y = gamble), color = "blue") +
geom_line(data = data_plt, aes(x = income, y = fit), color = "red", linewidth = 1) +
geom_ribbon(data = data_plt, aes(x = income, ymin = lower, ymax = upper), alpha = 0.5) +
labs(title = "GAM Spline (95% CI)", x = "income", y = "$ gambled") +
theme_minimal()
data(meatspec, package = "faraway")
train <- 1:150
test <- 151:215
trainmeat <- meatspec[train,]
testmeat <- meatspec[test,]
lmFit <- lm(fat ~ ., data = trainmeat)
# Define a function to calculate RMSE
rmse <- function(pred, obs) sqrt(mean((pred - obs)^2))
# Computing RMSE for the training set
rmse(fitted(lmFit), trainmeat$fat)
# RMSE for test set
rmse(predict(lmFit, newdata = testmeat), testmeat$fat)
# fat is reponse var, only include in y
x_train <- as.matrix(trainmeat[, -1])
y_train <- trainmeat$fat
x_test <- as.matrix(testmeat[, -1])
y_test <- testmeat$fat
# cross validation with alpha = 0 for ridge regression
cv_ridge <- cv.glmnet(x_train, y_train, alpha = 0, standardize = TRUE)
# getting best lambda
best_lambda <- cv_ridge$lambda.min
print(c("Best Lambda: ", best_lambda))
# predict on training
train_pred <- predict(cv_ridge, s = best_lambda, newx = x_train)
train_rmse <- rmse(train_pred, y_train)
print(c("Ridge Regression Training RMSE: ", train_rmse))
# pred on test set
test_pred <- predict(cv_ridge, s = best_lambda, newx = x_test)
test_rmse <- rmse(test_pred, y_test)
print(c("Ridge Reg. Test RMSE: ", test_rmse))
# now using alpha = 1 for lasso regression
cv_lasso <- cv.glmnet(x_train, y_train, alpha = 1, standardize = TRUE)
# getting new best lambda
best_lambda <- cv_lasso$lambda.min
print(c("Best Lambda: ", best_lambda))
# predict on training
train_pred <- predict(cv_lasso, s = best_lambda, newx = x_train)
train_rmse <- rmse(train_pred, y_train)
print(c("Lasso Training RMSE: ", train_rmse))
# pred on test set
test_pred <- predict(cv_lasso, s = best_lambda, newx = x_test)
test_rmse <- rmse(test_pred, y_test)
print(c("Lasso Test RMSE: ", test_rmse))
x_full <- as.matrix(meatspec[, -1])
y_full <- meatspec$fat
lasso_full <- glmnet(x_full, y_full, alpha = 1, lambda = best_lambda, standardize = TRUE)
# get coeff
lasso_coefs <- coef(lasso_full)
non_zero_coeff <- sum(lasso_coefs[-1, , drop = FALSE] != 0)  # -1 to remove predictor
selected_preds <- rownames(lasso_coefs)[which(lasso_coefs[-1, , drop = FALSE] != 0) + 1]
print(c("# of non-zero coefficients: ", non_zero_coeff))
print(c("non-zero coefficients: ", selected_preds))
x_full <- as.matrix(meatspec[, -1])
y_full <- meatspec$fat
lasso_full <- glmnet(x_full, y_full, alpha = 1, lambda = best_lambda, standardize = TRUE)
# get coeff
lasso_coefs <- coef(lasso_full)
non_zero_coeff <- sum(lasso_coefs[-1, , drop = FALSE] != 0)  # -1 to remove predictor
selected_preds <- rownames(lasso_coefs)[which(lasso_coefs[-1, , drop = FALSE] != 0) + 1]
print(c("# of non-zero coefficients: ", non_zero_coeff))
print(c("non-zero coefficients: ", selected_preds))
colnames(x_full)
x_full <- as.matrix(meatspec[, !(names(meatspec) %in% "fat")])
y_full <- meatspec$fat
lasso_full <- glmnet(x_full, y_full, alpha = 1, lambda = best_lambda, standardize = TRUE)
# get coeff
lasso_coefs <- coef(lasso_full)
non_zero_coeff <- sum(lasso_coefs[-1, , drop = FALSE] != 0)  # -1 to remove predictor
selected_preds <- rownames(lasso_coefs)[which(lasso_coefs[-1, , drop = FALSE] != 0) + 1]
print(c("# of non-zero coefficients: ", non_zero_coeff))
print(c("non-zero coefficients: ", selected_preds))
x_full <- as.matrix(meatspec[, !(names(meatspec) %in% "fat")])
y_full <- meatspec$fat
lasso_full <- glmnet(x_full, y_full, alpha = 1, lambda = best_lambda, standardize = TRUE)
# get coeff
lasso_coefs <- coef(lasso_full)
non_zero_coeff <- sum(lasso_coefs[-1, , drop = FALSE] != 0)  # -1 to remove predictor
selected_preds <- rownames(lasso_coefs)[which(lasso_coefs[-1, , drop = FALSE] != 0) + 1]
print(c("# of non-zero coefficients: ", non_zero_coeff))
print(c("non-zero coefficients: ", selected_preds))
colnames(x_full)
x_full <- as.matrix(meatspec[, !(names(meatspec) %in% "fat")])
y_full <- meatspec$fat
lasso_full <- glmnet(x_full, y_full, alpha = 1, lambda = best_lambda, standardize = TRUE)
# get coeff
lasso_coefs <- coef(lasso_full)
non_zero_coeff <- sum(lasso_coefs[-1, , drop = FALSE] != 0)  # -1 to remove predictor
selected_preds <- rownames(lasso_coefs)[which(lasso_coefs[-1, , drop = FALSE] != 0) + 1]
print(c("# of non-zero coefficients: ", non_zero_coeff))
print(c("non-zero coefficients: ", selected_preds))
x_full <- as.matrix(meatspec[, !(names(meatspec) %in% "fat")])
y_full <- meatspec$fat
lasso_full <- glmnet(x_full, y_full, alpha = 1, lambda = best_lambda, standardize = TRUE)
# get coeff
lasso_coefs <- coef(lasso_full)
non_zero_coeff <- sum(lasso_coefs[-1, , drop = FALSE] != 0)  # -1 to remove predictor
selected_preds <- lasso_coefs_tbl %>%
filter(predictor != "(Intercept)", .[[2]] != 0) %>%
pull(predictor)
x_full <- as.matrix(meatspec[, !(names(meatspec) %in% "fat")])
y_full <- meatspec$fat
lasso_full <- glmnet(x_full, y_full, alpha = 1, lambda = best_lambda, standardize = TRUE)
# get coeff
lasso_coefs <- coef(lasso_full)
non_zero_coeff <- sum(lasso_coefs[-1, , drop = FALSE] != 0)  # -1 to remove predictor
selected_preds <- rownames(lasso_coefs)[which(lasso_coefs[-1, , drop = FALSE] != 0) + 1]
print(c("# of non-zero coefficients: ", non_zero_coeff))
print(c("non-zero coefficients: ", selected_preds))
x_full <- as.matrix(meatspec[, !(names(meatspec) %in% "fat")])
y_full <- meatspec$fat
lasso_full <- glmnet(x_full, y_full, alpha = 1, lambda = best_lambda, standardize = TRUE)
# get coeff
lasso_coefs <- coef(lasso_full)
non_zero_coeff <- sum(lasso_coefs[-1, , drop = FALSE] != 0)  # -1 to remove predictor
selected_preds <- rownames(lasso_coefs)[which(lasso_coefs[-1, , drop = FALSE] != 0) + 1]
print(c("# of non-zero coefficients: ", non_zero_coeff))
print(c("non-zero coefficients: ", selected_preds))
